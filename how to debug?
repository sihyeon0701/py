# how to debug?
# 1. expain the problem
# 2. produce the bug again to see what went wrong
# 3. evalute the line by line 
# 4. Check the underline portion
# 5. Use print()
# 6. Fix the error

# for example

def cumulative_sum(num) :
  total = 0
  for i in range(1, num + 1) :
    print(i) # 좋은방법 중 하나인 print() 사용
    total += i
  return total

print(cumulative_sum(10))
