from Car import Car as c # Car라는 파일안에 Car라는 클래스 찾기

tesla = c("green", "electric")
print(vars(tesla))
tesla.start_engine()
print(vars(tesla))
for i in range(1, 100):
  tesla.speed_up(1)
  print(tesla.speed)

for j in range(1, 100):
  tesla.speed_down(1)
  print(tesla.speed)
